[Content]
ExtraTrees=mkosi.extra

Packages=udev

[Host]
# GCP TDX uses hv_netvsc which gets a predictable name (e.g. ens4), not eth0.
# Force old-style naming so network-setup.service's hardcoded eth0 works.
KernelCommandLineExtra=net.ifnames=0 biosdevname=0
