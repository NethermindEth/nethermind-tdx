[Content]
ExtraTrees=mkosi.extra

Packages=udev

# GCP TDX uses hv_netvsc which gets a predictable interface name (e.g. ens4),
# not eth0. Force old-style naming so network-setup.service works unchanged.
KernelCommandLine=net.ifnames=0 biosdevname=0
